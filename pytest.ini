[pytest]
DJANGO_SETTINGS_MODULE = backend.settings
pythonpath = backend
# Deliberately excludes backend/api_test.py — that is the live-server integration
# script (README § Testing), not a unit suite; it needs a running server.
python_files = tests.py test_*.py
testpaths = backend
addopts = -q
